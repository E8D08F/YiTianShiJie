/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // MARK: `swcMinify` isn't considered a valid option after Next.js upgrade
  // swcMinify: true,
}

module.exports = nextConfig
